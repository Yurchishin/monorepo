const CSS = /\.css$/

const CSS_MODULE = /\.module\.css$/

const SASS = /\.(scss|sass)$/

const SASS_MODULE = /\.module\.(scss|sass)$/

module.exports = {
  CSS,
  CSS_MODULE,
  SASS,
  SASS_MODULE,
}
