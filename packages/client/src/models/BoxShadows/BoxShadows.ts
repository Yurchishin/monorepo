import types from 'mst-types'

const BoxShadows = types.model({
    XS: types.string,
    S: types.string,
    M: types.string,
    L: types.string,
    XL: types.string,
    NONE: types.string,
})

export default BoxShadows
